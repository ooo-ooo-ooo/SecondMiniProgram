<!--pages/home/home.wxml-->
<button open-type="getUserInfo" bindgetuserinfo="handleUserInfo" wx:if="{{isShow}}">获取信息</button>
<view wx:else>
        <image src="{{userInfo.avatarUrl}}"></image>
        <view>{{userInfo.nickName}}</view>
</view>
